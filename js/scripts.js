/*
function s1() {
    $("body").scrollTo("#B", 1000);
};

var moved = false;

var active = $("#A");

var unit = $(active).height() * 0.65;

$(document).scroll(function () {
    var scroll = $(document).scrollTop();
    console.log(scroll);
    if (scroll >= unit) {
        var moved = true;
    }

    var moved = false;
});
*/


